__pycache__/
*.pyc
out.txt
bench.md
dist/

# Doc pipeline — generated staging dirs and local-browse html.
# The committed markdown tree is docs/, promoted via `make docs-publish`.
_docs_staging/
_site_staging/
_site/

# v0.8.0 CX Data and Code Language Guide — build output. Generated by
# `make guide` from docs-src/canonical/ (see scripts/gen_guide/README.md).
docs/guide/

# Synthesized bench fixtures (regenerate via scripts/gen_bench_fixture.py)
fixtures/bench/bench_1mb.cx
fixtures/bench/bench_10mb.cx
fixtures/bench/bench_100mb.cx

.claude/settings.local.json

# Python venvs (per-binding optional deps like pyarrow)
lang/python/.venv/

# V / Rust build artifacts
vcx/target/
vcx/cx/*.dylib
vcx/code/*.dylib
vcx/code/*.so
vcx/cx/*.so
vcx/tests/*.dylib
vcx/tests/*.so
lang/rust/cxlib/target/
lang/v/.cache/

# compiled V binaries
vcx/tests/conformance_run
vcx/tests/runners/conformance/conformance_run
lang/v/examples/transform
lang/v/examples/demo

# Language binding build artifacts (active)
lang/rust/cxlib/target/

# Archived binding build artifacts (lang/_archived/) — not compiled against
# v0.8.0 ABI; artifacts from prior development kept out of tracking.
lang/_archived/**/bin/
lang/_archived/**/obj/
lang/_archived/**/target/
lang/_archived/**/.gradle/
lang/_archived/**/build/
lang/_archived/**/.build/
lang/_archived/**/node_modules/

# Editor tooling build artifacts
tooling/lsp/node_modules/
tooling/lsp/out/
tooling/vscode/node_modules/
tooling/vscode/out/
tooling/vscode/*.vsix

# v0.7.0 CC fuzz harness — crashes are runtime artifacts, never source
vcx/fuzz/crashes/

# Conformance runner build artifact (V binary co-located with source)
vcx/tests/runners/eval/eval_conform

# CCD spawned-task worktrees
.claude/
vcx/tests/runners/code_modify_sharing_bench
