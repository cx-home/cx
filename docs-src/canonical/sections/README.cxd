[# Section files for cx-data-and-code-guide.

   Each section listed in ../manifest.cxd is one file here. Files are
   authored in CX. The template (template.cx, deferred) walks the
   manifest and emits the rendered guide.

   STATUS: directory is a scaffold. Content authoring begins once the
   guide can be rendered with full sectional structure (the same
   blocker affecting docs-src/content/ pages today — see backlog.cx
   i-doc-gen-sectional-regression).

   Naming: NN-slug.cx matching the manifest's :file attribute.
   E.g., manifest's [section :n 1 :id intro :file '01-intro.cx']
   maps to ./01-intro.cx. #]

[readme
  status='scaffold — no section files authored yet'
  see-also=../manifest.cxd]
