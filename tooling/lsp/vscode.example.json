{
  "_comment": "VS Code: drop into .vscode/settings.json (or User Settings) and reload window. Requires a generic LSP-client extension such as 'mattn.lsp' or 'fwcd.kotlin' style wrapper, OR the official cx-vscode extension when published. `cx` must be on PATH.",
  "languageServerExample": {
    "cx": {
      "command": "cx",
      "args": ["lsp"],
      "filetypes": ["cx"],
      "rootPatterns": [".git/", ".cxlint.cx"]
    }
  },
  "files.associations": {
    "*.cx": "cx",
    "*.cxd": "cx",
    "*.cxs": "cx"
  }
}
