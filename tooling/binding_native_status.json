{
  "comment": "v0.7.6: per-binding native-implementation dashboard. Tracks which v0.7.6 program-evaluator-surface features each binding's native path covers vs which routes through the libcx C ABI. The v0.7.6 evaluator family is cx_code_eval / cx_code_eval_with_len / cx_code_eval_streaming / cx_code_diagram (spec/abi.md §2.16.1 + §2.16.2). Tier 1 (V / Python / Go) is c-abi-passthrough by design and locked at parity per v0.7.6 gate 11. Tier 2 (Rust / TypeScript) catch up once per release.",
  "version": "v0.7.6",
  "as_of": "2026-05-21",
  "bindings": {
    "v": {
      "tier": 1,
      "implementation": "native",
      "comment": "V is the native reference implementation. cx_code_eval and the entire code/ module live in vcx/code/; every feature is native by definition.",
      "features": "*"
    },
    "python": {
      "tier": 1,
      "implementation": "c-abi-passthrough",
      "native_coverage": ["data-bin", "ast-parse", "to-cx", "to-xml", "to-json", "to-yaml", "to-toml", "to-md"],
      "ffi_coverage": ["cx_code_eval", "cx_code_eval_with_len", "cx_code_eval_streaming", "cx_code_diagram", "cx_diff", "cx_hash", "cx_canonical", "cx_arrow_*", "cx_data_bin_chunked"],
      "comment": "Native Python parser for data-bin + AST surfaces; program-eval / diff / hash / canonical via libcx FFI. Arrow + Parquet via pyarrow C-Data bridge. 73/73 cross-binding conformance parity per v0.7.6 gate 11."
    },
    "go": {
      "tier": 1,
      "implementation": "c-abi-passthrough",
      "native_coverage": ["data-bin", "ast-parse", "to-cx", "to-xml", "to-json"],
      "ffi_coverage": ["cx_code_eval", "cx_code_eval_with_len", "cx_code_eval_streaming", "cx_code_diagram", "cx_diff", "cx_hash", "cx_canonical", "cx_arrow_*", "cx_data_bin_chunked"],
      "comment": "Native Go parser for data-bin + AST + emitters. Program-eval via cgo + libcx; cgo trampoline (cxGoStreamTrampoline) wired to cx_code_write_cb. 73/73 cross-binding conformance parity."
    },
    "rust": {
      "tier": 2,
      "implementation": "c-abi-passthrough",
      "native_coverage": ["data-bin", "ast-parse"],
      "ffi_coverage": ["cx_code_eval", "cx_code_eval_with_len", "cx_code_eval_streaming", "cx_code_diagram", "cx_diff", "cx_hash", "cx_canonical", "cx_arrow_*", "cx_data_bin_chunked", "to_*"],
      "comment": "Native data-bin + AST; everything else routes through libcx FFI via ensure_thread(). Tier 2 catchup at v0.7.6: surface migration from cx_eval to cx_code_eval is pending (filed as v0.7.6 follow-up)."
    },
    "typescript": {
      "tier": 2,
      "implementation": "c-abi-passthrough",
      "native_coverage": ["data-bin", "ast-parse"],
      "ffi_coverage": ["cx_code_eval", "cx_code_eval_with_len", "cx_code_eval_streaming", "cx_code_diagram", "cx_diff", "cx_hash", "cx_canonical", "cx_arrow_*", "to_*"],
      "comment": "koffi FFI to libcx for most surfaces. Arrow + Parquet via apache-arrow JS IPC bridge (W7 / X6) — composes with libcx C-Data export. Tier 2 catchup at v0.7.6: surface migration from cx_eval to cx_code_eval is pending."
    }
  },
  "frozen_bindings": {
    "csharp": {"tier": 3, "status": "frozen", "reason": "Not in Tier-1 / Tier-2 parity matrix at v0.7.6"},
    "java":   {"tier": 3, "status": "frozen", "reason": "Same"},
    "kotlin": {"tier": 3, "status": "frozen", "reason": "Same"},
    "ruby":   {"tier": 3, "status": "frozen", "reason": "Same"},
    "swift":  {"tier": 3, "status": "frozen", "reason": "Same"}
  },
  "v0_7_x_native_port_roadmap": {
    "comment": "v0.7.x / v0.8.0 candidates for native-implementation expansion. Order: data-bin → emitters → cx_code_eval (the latter is large and binding-specific — the v0.7.6 program evaluator is a substantial body of work to port natively).",
    "priority_order": [
      "rust.cx_code_eval surface migration (Tier 2 catchup; FFI signature change, not native port)",
      "typescript.cx_code_eval surface migration (Tier 2 catchup; FFI signature change, not native port)",
      "go.cx_diff (native Go diff)",
      "python.cx_diff (native Python diff)",
      "rust.cx_canonical (native Rust canonical)"
    ]
  }
}
